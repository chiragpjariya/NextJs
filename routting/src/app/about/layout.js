export default function Layout({ children }) {
    return (
        <div>
            <h1 className="ml-[40%] mb-20 mt-10 text-[30px] font-semibold">Comment layout for about us</h1>
            {children}
        </div>
    )
}